<Window
    x:Class="Couno.ConfigureBoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Couno"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ConfigureBoardView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type={x:Type local:ConfigureBoardViewModel}}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ItemsControl Grid.Row="0" ItemsSource="{Binding Abilities}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <WrapPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button Command="{Binding AddTokenCommand}" Content="Add Token" />
            <Button Content="Remove Red" />
        </WrapPanel>

        <WrapPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button Content="Confirm" Click="ConfirmConfiguration" />
            <Button Content="Cancel" />
        </WrapPanel>
    </Grid>
</Window>
